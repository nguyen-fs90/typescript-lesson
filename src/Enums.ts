/**
 * Numeric enums
 */
enum DirectionNumber {
  Up = 1,
  Down,
  Left,
  Right,
}

/**
 * String enums
 */
enum DirectionString {
  Up = "UP",
  Down = "DOWN",
  Left = "LEFT",
  Right = "RIGHT",
}